

# Hướng dẫn cài đặt

``` shell
apt-get install -y git fish
cd /opt
mkdir makiposiot
cd makiposiot
git clone https://github.com/makipos/iot-server.git
cd iot-server
chmod -R +x script
chmod -R +x scriptdeploy
chmod +x install.sh
./install.sh
```
